import unittest

from src.global import globals

class TestGlobal(unittest.TestCase):
    
    def test_create_entry(self):
        pass

    def test_entry():
        e = entry("lexptr",0)
        symboltable.append(e)
        print(f'symboltable : {symboltable[0].lexptr, symboltable[0].token }')
